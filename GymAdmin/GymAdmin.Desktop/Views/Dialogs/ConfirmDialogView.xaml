<UserControl x:Class="GymAdmin.Desktop.Views.Dialogs.ConfirmDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource BackgroundWhiteBrush}"
            CornerRadius="8"
            Padding="20"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            Width="420">
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Margin="0 0 0 10"/>
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0 0 0 20"/>
           
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{Binding CancelText}" 
                        Margin="0 0 8 0" Width="90"
                        Style="{Binding CancelButtonStyle}"
                        Command="{Binding CancelCommand}"
                        Visibility="{Binding ShowCancelButton, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <Button Content="{Binding AcceptText}" Width="90"
                        Style="{Binding AcceptButtonStyle}"
                        Command="{Binding AcceptCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>