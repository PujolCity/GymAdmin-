<UserControl x:Class="GymAdmin.Desktop.Views.Configuraciones.MetodoPagoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:GymAdmin.Desktop.Views.Configuraciones"
             xmlns:viewmodels="clr-namespace:GymAdmin.Desktop.ViewModels.Configuracion"
             mc:Ignorable="d" 
             Background="Transparent"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Margin="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" 
        Padding="10" 
        CornerRadius="6" Margin="0 0 0 9">
                    <!-- Toolbar “elástica” -->
                    <WrapPanel Orientation="Horizontal" VerticalAlignment="Center" ItemHeight="36">
                        <!-- Lupa -->
                        <fa:IconImage Icon="Search"
                      Foreground="{StaticResource TextInvertedBrush}"
                      VerticalAlignment="Center"
                      Width="18" Height="18"
                      Margin="0,0,10,0"/>

                        <!-- Búsqueda (con botón limpiar superpuesto) -->
                        <Grid MinWidth="160" MaxWidth="260" Width="160" Margin="0,0,8,0" VerticalAlignment="Center">
                            <TextBox x:Name="SearchTextBox"
                     Tag="Buscar por nombre…"
                     Text="{Binding TextoFiltro, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource SearchTextBoxStyle}"
                     Height="36" />
                        </Grid>
                        <Button 
               Style="{StaticResource ClearSearchButtonStyle}"
               Command="{Binding LimpiarFiltroCommand}"
               HorizontalAlignment="Right"
               VerticalAlignment="Center"
               Margin="0,0,6,0"
               Visibility="{Binding Text, ElementName=SearchTextBox,
                             Converter={StaticResource EmptyStringToVisibilityConverter},
                             UpdateSourceTrigger=PropertyChanged}">
                            <fa:IconImage Icon="Times" Width="14" Height="14" Foreground="{StaticResource TextInvertedBrush}"/>
                        </Button>

                        <ComboBox ItemsSource="{Binding Estados}"
                  SelectedItem="{Binding EstadoSeleccionado}"
                  Style="{StaticResource FilterComboBoxStyle}"
                  Width="100"
                  Margin="8,0,0,0"/>

                        <Border Width="0" Margin="0" HorizontalAlignment="Right"/>
                        <Button Margin="12,0,0,0"
                MinWidth="100" Height="33"
                Command="{Binding NuevoMetodoPagoCommand}"
                Style="{StaticResource PrimaryButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <fa:IconImage Icon="Plus" Margin="0 0 8 0" Width="16" Height="16"/>
                                <TextBlock Text="Nuevo" FontSize="14"/>
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </Border>
                <!-- Header -->
                <StackPanel Grid.Row="1" Margin="0,0,0,12">


                    <!-- Banner de error -->
                    <Border Background="#FFfdecea"
         BorderBrush="#FFf5c2c7"
         BorderThickness="1"
         CornerRadius="6"
         Padding="8"
         Margin="0,8,0,0"
         Visibility="{Binding ErrorMessage, Converter={StaticResource NullOrEmptyToVisibilityConverter}}">
                        <DockPanel LastChildFill="True">
                            <TextBlock Text="{Binding ErrorMessage}"
                        Foreground="#FF58151c"
                        TextWrapping="Wrap" />
                            <Button Content="✕"
                     DockPanel.Dock="Right"
                     Margin="6,0,0,0"
                     Padding="6,0"
                     Width="32"
                     Style="{StaticResource DangerButtonStyle}"    
                     Command="{Binding ClearErrorCommand}"/>
                        </DockPanel>
                    </Border>
                </StackPanel>

                <TextBlock Grid.Row="2" Margin="0 0 0 10"
             Text="{Binding TotalCount, StringFormat='Pagos encontrados: {0}'}"
             Foreground="{StaticResource TextSecondaryBrush}"
             FontSize="13" FontWeight="SemiBold"/>

                <!-- LISTA -->
                <DataGrid Grid.Column="0"
                      Grid.Row="2"
                      ItemsSource="{Binding MetodosPago}"
                      SelectedItem="{Binding MetodoPagoSeleccionado}"
                      Sorting="DataGrid_Sorting"
                      IsReadOnly="True"
                      Style="{StaticResource BaseDataGridStyle}"
                      HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.CanContentScroll="True">

                    <DataGrid.Resources>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="8 6"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <!-- NOMBRE -->
                        <DataGridTextColumn Header="NOMBRE"
                       Binding="{Binding Nombre}"
                       Width="2*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource CenteredCellText}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- AJUSTE -->
                        <DataGridTextColumn Header="AJUSTE"
                       Binding="{Binding ValorAjuste}"
                       Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource CenteredCellText}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- ESTADO -->
                        <DataGridTemplateColumn Header="ESTADO" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Width="14" Height="14" CornerRadius="7"
                Background="{Binding Estado, Converter={StaticResource StatusToBrushConverter}}"
                HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Border.ToolTip>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Estado}" FontWeight="Bold"/>
                                            </StackPanel>
                                        </Border.ToolTip>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- ORDEN -->
                        <DataGridTextColumn Header="ORDEN"
                       Binding="{Binding Orden}"
                       Width="Auto">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource CenteredCellText}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- ACCIONES -->
                        <!--<DataGridTemplateColumn Header="ACCIONES" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                        -->
                        <!-- SELECCIONAR PARA EDITAR (opcional: ctrl de UX) -->
                        <!--
                                        <Button Style="{StaticResource DataGridActionButtonStyle}"
                           Command="{Binding DataContext.DeletePaymentMethodCommand,
                                             RelativeSource={RelativeSource AncestorType=DataGrid}}"
                           CommandParameter="{Binding}"
                           ToolTip="Eliminar">
                                            <fa:IconImage Icon="Trash" Width="16" Height="16"
                                     Foreground="{StaticResource SecondaryBrush}"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>-->
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Paginación (como en Socios) -->
                <Border Grid.Row="3" Background="{StaticResource BackgroundLightBrush}" Padding="10" CornerRadius="4" Margin="0 10 0 0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}">
                        <Run Text="Mostrando "/>
                        <Run Text="{Binding DisplayFrom, Mode=OneWay}"/>
                        <Run Text=" – "/>
                        <Run Text="{Binding DisplayTo, Mode=OneWay}"/>
                        <Run Text=" de "/>
                        <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                        </TextBlock>

                        <WrapPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <ComboBox Width="90"
                                  ItemsSource="{Binding PageSizes, Mode=OneWay}"
                                  SelectedItem="{Binding PageSize}"
                                  Style="{StaticResource FilterComboBoxStyle}"
                                  ToolTip="Elementos por página" Height="32" Margin="0,0,6,0"/>
                            <Button Content="⏮" Command="{Binding GoFirstPageCommand}" Width="36" Height="32" Margin="0,0,4,0"/>
                            <Button Content="◀"  Command="{Binding GoPrevPageCommand}"  Width="36" Height="32" Margin="0,0,8,0"/>
                            <TextBlock VerticalAlignment="Center" Margin="6,0">
                            <Run Text="Pág. "/>
                            <Run Text="{Binding PageNumber, Mode=OneWay}"/>
                            <Run Text=" / "/>
                            <Run Text="{Binding TotalPages, Mode=OneWay}"/>
                            </TextBlock>
                            <Button Content="▶"  Command="{Binding GoNextPageCommand}"  Width="36" Height="32" Margin="8,0,4,0"/>
                            <Button Content="⏭" Command="{Binding GoLastPageCommand}"  Width="36" Height="32" Margin="0,0,10,0"/>
                        </WrapPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- EDITOR -->
        <Grid Grid.Column="1"
                 Margin="12"
                 DataContext="{Binding MetodoPagoSeleccionado}">

            <GroupBox Header="Editar método de pago">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Nombre -->
                        <RowDefinition Height="Auto"/>
                        <!-- Activo -->
                        <RowDefinition Height="Auto"/>
                        <!-- Tipo ajuste -->
                        <RowDefinition Height="Auto"/>
                        <!-- Valor ajuste -->
                        <RowDefinition Height="Auto"/>
                        <!-- Orden -->
                        <RowDefinition Height="*"/>
                        <!-- Espacio flexible -->
                        <RowDefinition Height="Auto"/>
                        <!-- Botones CRUD -->
                        <RowDefinition Height="Auto"/>
                        <!-- Subir / Bajar -->
                    </Grid.RowDefinitions>

                    <!-- NOMBRE -->
                    <TextBlock Grid.Row="0" Text="Nombre"/>
                    <TextBox Grid.Row="0" Margin="0,20,0,0"
                Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}"/>

                    <!-- ACTIVO -->
                    <StackPanel Grid.Row="1" Margin="0,12,0,0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Activo" VerticalAlignment="Center" Margin="0,0,10,0"/>

                        <ToggleButton Style="{StaticResource ToggleSwitchStyle}"
                  IsChecked="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  ToolTip="{Binding IsActive, Converter={StaticResource BoolToActivoInactivoConverter}}"
                  IsEnabled="{Binding DataContext.IsBusy,
                                      RelativeSource={RelativeSource AncestorType=UserControl},
                                      Converter={StaticResource InverseBoolConverter}}"/>
                    </StackPanel>

                    <!-- TIPO AJUSTE -->
                    <StackPanel Grid.Row="2" Margin="0,12,0,0">
                        <TextBlock Text="Tipo de ajuste"/>
                        <ComboBox
        ItemsSource="{Binding DataContext.TiposAjusteE,
                              RelativeSource={RelativeSource AncestorType=UserControl}}"
        SelectedItem="{Binding TipoAjuste, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            />
                    </StackPanel>

                    <!-- VALOR AJUSTE -->
                    <StackPanel Grid.Row="3" Margin="0,12,0,0">
                        <TextBlock Text="Valor ajuste"/>
                        <TextBox Text="{Binding ValorAjuste}" />
                    </StackPanel>

                    <!-- ORDEN + SUBIR / BAJAR -->
                    <StackPanel Grid.Row="4" Margin="0,12,0,0">
                        <TextBlock Text="Orden"/>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBox Text="{Binding Orden}" VerticalAlignment="Center"          
                                     TextAlignment="Center"
                                     Width="60" IsReadOnly="True"/>

                            <Button Content="▲"
                                       Width="30"
                                       Height="24"
                                       Margin="4,0,0,0"
                                       Command="{Binding DataContext.MoveUpCommand,
                                   RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       Style="{StaticResource PrimarySmallButtonStyle}"/>

                            <Button Content="▼"
                                       Width="30"
                                       Height="24"
                                       Margin="4,0,0,0"
                                       Command="{Binding DataContext.MoveDownCommand,
                                   RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       Style="{StaticResource PrimarySmallButtonStyle}"/>

                        </StackPanel>
                    </StackPanel>

                    <!-- ESPACIADOR VERTICAL -->
                    <Grid Grid.Row="5"/>

                    <!-- CRUD -->
                    <StackPanel Grid.Row="6"
                   Orientation="Horizontal"
                   HorizontalAlignment="Right"
                   Margin="0,12,0,0">

                        <Button Content="Guardar"
                                Command="{Binding DataContext.GuardarCommand,
                                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Style="{StaticResource PrimarySmallButtonStyle}"/>

                        <Button Content="Eliminar"
                                Command="{Binding DataContext.EliminarCommand,
                                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Style="{StaticResource DangerButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

        </Grid>
    </Grid>
</UserControl>
